export interface Teacher {
    id ?: number,
    user_id ? : number,
    first_name ?:string,
    last_name ?:string,
    email ?:string,
    phone ? : string,
    birth_date ? : Date,
    role ?:string;
  }
  